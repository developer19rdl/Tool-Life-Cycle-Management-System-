<Window x:Class="CalibrationResultSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calibration Result Summary" Height="700" Width="1200"
        WindowStartupLocation="CenterOwner" Background="#F1F5F9">
    <Window.Resources>
        <!-- Light grey column header style -->
        <Style x:Key="SummaryHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#E8E8E8"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="4,6"/>
            <Setter Property="BorderBrush" Value="#999999"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <!-- Cell style with thin black gridlines -->
        <Style x:Key="SummaryCellStyle" TargetType="DataGridCell">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border BorderBrush="#BBBBBB" BorderThickness="0,0,1,1" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="3,2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSpacer}" Value="True">
                    <Setter Property="BorderThickness" Value="0"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#F8FAFC">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Panel -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="25,20">
            <Grid>
                <StackPanel>
                    <TextBlock x:Name="TxtTitle" Text="Instrument Name" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
                    <TextBlock x:Name="TxtTotalDone" Text="Total Calibration Done: 0" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>

                <Button x:Name="BtnExportExcel" Content="Export to Excel" HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Width="140" Height="38" Background="#2563EB" Foreground="White" FontWeight="SemiBold"
                        Style="{StaticResource PrimaryButtonStyle}" Click="BtnExportExcel_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <!-- Content Area: Dynamic Tab Control -->
        <TabControl x:Name="TabSummary" Grid.Row="1" Margin="20" Background="White" BorderBrush="#CCCCCC" BorderThickness="1">
            <!-- Tabs are built dynamically in code-behind -->
        </TabControl>
    </Grid>
</Window>

