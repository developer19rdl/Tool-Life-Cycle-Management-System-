<Window x:Class="LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login - Honda Application" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border Background="White" CornerRadius="20" BorderBrush="#E2E8F0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Logo/Title -->
                <RowDefinition Height="*" />    <!-- Form -->
                <RowDefinition Height="Auto" /> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
                <TextBlock Text="Welcome Back" FontSize="28" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Center"/>
                <TextBlock Text="Log in to your account" FontSize="14" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Form -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="Username" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,8"/>
                <Border Background="#F8FAFC" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10,5" Margin="0,0,0,20">
                    <TextBox x:Name="TxtUsername" BorderThickness="0" Background="Transparent" FontSize="16" Foreground="#1E293B" VerticalContentAlignment="Center" Height="30"/>
                </Border>

                <TextBlock Text="Password" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,8"/>
                <Border Background="#F8FAFC" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10,5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <PasswordBox x:Name="TxtPassword" Grid.Column="0" BorderThickness="0" Background="Transparent" FontSize="16" Foreground="#1E293B" VerticalContentAlignment="Center" Height="30"/>
                        <TextBox x:Name="TxtPasswordVisible" Grid.Column="0" BorderThickness="0" Background="Transparent" FontSize="16" Foreground="#1E293B" VerticalContentAlignment="Center" Height="30" Visibility="Collapsed"/>
                        
                        <Button x:Name="BtnShowPassword" Grid.Column="1" Click="BtnShowPassword_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="5,0" VerticalAlignment="Center">
                            <TextBlock x:Name="IconShowPassword" Text="👁️" FontSize="16" Foreground="#64748B"/>
                        </Button>
                    </Grid>
                </Border>
                
                <TextBlock x:Name="LblError" Foreground="#EF4444" FontSize="12" HorizontalAlignment="Center" Margin="0,15,0,0" Visibility="Collapsed"/>

                <Button Content="Login" Click="BtnLogin_Click" Margin="0,30,0,0" Height="45" Background="#3B82F6" Foreground="White" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <!-- Footer -->
            <Button Grid.Row="2" Content="Close Application" Click="BtnClose_Click" Background="Transparent" Foreground="#64748B" BorderThickness="0" Cursor="Hand" FontSize="12"/>
        </Grid>
    </Border>
</Window>
