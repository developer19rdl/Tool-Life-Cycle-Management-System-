<Window x:Class="AddEditInstrumentSizeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Instrument Size" 
        SizeToContent="Height" 
        Width="700"
        WindowStartupLocation="CenterOwner"
        Background="White"
        ResizeMode="NoResize"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    
    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="#475569" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,10,0" />
        </Style>
        
        <Style x:Key="InputStyle" TargetType="Control">
            <Setter Property="Height" Value="40" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="BorderBrush" Value="#CBD5E1" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="24,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2563EB" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1D4ED8" />
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F8FAFC" />
            <Setter Property="Foreground" Value="#64748B" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="24,10" />
            <Setter Property="BorderBrush" Value="#E2E8F0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F5F9" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#CBD5E1" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Title -->
            <RowDefinition Height="Auto" /> <!-- Subtitle -->
            <RowDefinition Height="Auto" /> <!-- Form -->
            <RowDefinition Height="Auto" /> <!-- Footer -->
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtTitle" Text="Add Instrument Size" FontSize="26" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,8" />
        <TextBlock Text="Size is shown in Add Instrument dropdown. GroupCode is used for ControlNo prefix (e.g., D1-001)." Foreground="#64748B" FontSize="14" Margin="0,0,0,40" TextWrapping="Wrap" Grid.Row="1" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Row 0: Instrument Type -->
            <Label Grid.Row="0" Grid.Column="0" Content="Instrument Type" Style="{StaticResource LabelStyle}" Margin="0,0,0,20" />
            <ComboBox Grid.Row="0" Grid.Column="1" x:Name="ComboType" Style="{StaticResource InputStyle}" DisplayMemberPath="TypeName" SelectedValuePath="TypeName" Margin="0,0,0,20" />

            <!-- Row 1: Size -->
            <Label Grid.Row="1" Grid.Column="0" Content="Size" Style="{StaticResource LabelStyle}" Margin="0,0,0,20" />
            <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtSize" Style="{StaticResource InputStyle}" Margin="0,0,0,20" />

            <!-- Row 2: GroupCode -->
            <Label Grid.Row="2" Grid.Column="0" Content="GroupCode" Style="{StaticResource LabelStyle}" Margin="0,0,0,20" />
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtGroupCode" Style="{StaticResource InputStyle}" Margin="0,0,0,20" />

            <!-- Row 3: SortOrder -->
            <Label Grid.Row="3" Grid.Column="0" Content="SortOrder" Style="{StaticResource LabelStyle}" Margin="0,0,0,20" />
            <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtSort" Style="{StaticResource InputStyle}" Margin="0,0,0,20" />

            <!-- Row 4: Active -->
            <CheckBox Grid.Row="4" Grid.Column="1" x:Name="CheckActive" Content="Active" IsChecked="True" Foreground="#475569" FontSize="14" VerticalContentAlignment="Center" />
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,40,0,0">
            <Button x:Name="BtnCancel" Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" Click="BtnCancel_Click" Margin="0,0,16,0" />
            <Button x:Name="BtnSave" Content="Save" Style="{StaticResource PrimaryButtonStyle}" Click="BtnSave_Click" />
        </StackPanel>
    </Grid>
</Window>
